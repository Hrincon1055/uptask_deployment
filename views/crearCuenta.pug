<!DOCTYPE html>
html(lang="en")
  head
    include partials/header
  //- end head  
  body(class="login")
    div(class="contenedor-formulario")
      h1 Crea tu cuenta en Uptask
      if locals.mensajes
        div(class="alertas")
          each categoria in Object.keys(locals.mensajes)
            each error in mensajes[categoria]
              div(class=`alerta ${categoria}`)
                p= error
              //- end div  
        //- end div        
      //- end if    
      form(id="formulario" class="caja caja-login" method="POST" action="/crear-cuenta")
        div(class="campo")
          label(for="email") E-mail:
          input(type="email" name="email" placeholder="Tu email" value= email)
        //- end div   
        div(class="campo")  
          label(for="password") Password:
          input(type="password" name="password" placeholder="Tu Password" value= password)
        //- end div   
        div(class="campo enviar")  
          input(type="submit" class="boton" value="Crear Cuenta")
        //- end div   
        div(class="campo")   
          a(href="/iniciar-session") Volver a Iniciar Sesión
        //- end div  
      //- end form
    //- end div
  //- end body
//- end html